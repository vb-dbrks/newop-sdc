// TODO: TypeChip + (SDC-only) RequiredDocRow + PromptTextarea + AttachButton + EnhanceButton + GenerateButton.
// Two modes (New Opportunity / SDC) per Figma. SDC requires a parent New Opp document.
export default function AuthoringComposer() {
  return null;
}
